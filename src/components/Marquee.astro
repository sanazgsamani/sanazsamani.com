<div class="py-[60px] bg-cream overflow-hidden whitespace-nowrap">
  <div class="text-banner-track">
    {['Mind', 'Body', 'Spirit', 'Discipline', 'Strength', 'Purpose', 'Self-Love', 'Growth', 'Mind', 'Body', 'Spirit', 'Discipline', 'Strength', 'Purpose', 'Self-Love', 'Growth'].map((word) => (
      <>
        <span class="font-serif font-light italic text-sage whitespace-nowrap" style="font-size: clamp(1.5rem, 3vw, 2.5rem);">{word}</span>
        <span class="text-wood" style="font-size: clamp(1.5rem, 3vw, 2.5rem);">&bull;</span>
      </>
    ))}
  </div>
</div>
